import { DefaultTheme } from "styled-components"

const bgColor = {
    main:'#EFEFEF',
    intro: '#FCFCFC',
}
const fontColor = {
    light_gray: '#D0D0D0',
    dark_gray:'#676767',
    highlight: '#7D97AE'
}

const theme:DefaultTheme = {
    bgColor,
    fontColor
}
export default theme